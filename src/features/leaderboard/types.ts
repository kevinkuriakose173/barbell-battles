export type LeaderboardEntry = {
  first_name: string;
  is_current_user: boolean;
  last_name: string;
  lift_count: number;
  rank: number;
  score_kg: number;
  user_id: string;
  username: string;
};
